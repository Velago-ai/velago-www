{
  "name": "velago-www",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "app",
    "lib/api-client-react",
    "lib/api-spec"
  ],
  "scripts": {
    "install:all": "npm install",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "dev": "npm run dev -w @workspace/velago-landing"
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}
